# (this is my real config for this repo)
# for an example file with everything explained open "fknode.example.yaml" instead
divineProtection: ["prettifier", "linter"]
destroy:
    intensities: ["*"]
    targets:
        - "dist"
commitActions: true
commitMessage: "Maintenance tasks (automated by FuckingNode itself!)" # lol
